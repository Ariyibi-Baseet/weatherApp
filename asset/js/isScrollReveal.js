// initialize
window.sr = ScrollReveal();

// scroll effect for some classes

// for landing section header
sr.reveal('w-landing',{
    duration:2000,
    origin: 'top',
    distance:'40px',
    easing:'ease',
    reset:true,
    opacity:0,
});
